# API origin (integration spec name)
VITE_API_URL=https://hr-back-iza2.vercel.app

# Legacy alias still supported in code
VITE_API_BASE_URL=https://hr-back-iza2.vercel.app

# Use `session` for sessionStorage tokens, omit or `local` for localStorage (default)
# VITE_AUTH_STORAGE=session
